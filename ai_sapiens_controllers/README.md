# AI Sapiens Controllers

This directory groups ROS 2 control-related packages for AI Sapiens.

`ai_sapiens_controllers` itself is not a ROS package and intentionally has no `package.xml`.
Each child directory is an independent ROS package discovered by `colcon`.

- `ai_sapiens_joint_group_position_controller`
- `ai_sapiens_rc_broadcaster`
